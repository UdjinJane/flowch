# ============================================================================
# ЧИСТЫЙ МАРШЕВЫЙ БАЗИС CHROMA-FP8-SCALED ПОД PYTHON 3.11 (RTX 3090 / APEX)
# ============================================================================

# 1. Главный калибр (Ядро вычислений CUDA 12.1)
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# 2. Квантовый контур плавки и экономии VRAM
torchao==0.7.0
bitsandbytes==0.43.3

# 3. Инфраструктурная обвязка и пайплайны инференса
diffusers==0.30.0
peft==0.11.1
transformers==4.43.3
accelerate==0.33.0
safetensors==0.4.4

# 4. Вспомогательная периферия и десериализаторы токенов
numpy==1.26.4
pillow==10.4.0
psutil==5.9.8
huggingface_hub==0.24.5
sentencepiece==0.2.0
protobuf==5.27.2
tiktoken==0.7.0

# ============================================================================
# СТРОГИЙ БОРТОВОЙ ЗАПРЕТ:
# optimum-quanto НЕ ставить — рубит рантайм С++ расширениями под Windows!
# ninja НЕ ставить — уводит ядро в PCIe-оверсвап и Shared VRAM!
# ============================================================================
