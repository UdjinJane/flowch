ПЕРФОКАРТА ВОССТАНОВЛЕНИЯ КОНТЕКСТА
Сессия: V02-STABLE
Цель: Устранение "песка" в генерации, стабилизация обучения.

1. ДЕЙСТВИЯ:
train_engine_v02.py:

Удалена искусственная масштабировка LoRA весов.
Добавлены проверки на NaN/Inf в лоссе и таймстепах.
Переведено вычисление лосса в bfloat16.
Добавлена валидация размерностей выходного тензора.
generate_v02.py:

Усилена проверка совместимости VAE весов (strict=True).
Добавлены проверки на NaN/Inf в латентных векторах.
Валидация размерностей после решейпинга.
lora_core_v02.py:

Удалено искусственное масштабирование LoRA весов.
Добавлено логирование фильтрации слоев при квантовании FP8.
Проверка точности LoRA весов (bfloat16).
dataset_v02.py:

Удален weights_only=True, добавлена проверка размерностей латентных векторов.
2. ЧТО ПРОВЕРИТЬ В СЛЕДУЮЩЕЙ ИТЕРАЦИИ:
Стабильность лосса:

Отсутствие скачков или NaN/Inf в лоссе.
Средний и максимальный градиенты должны быть стабильными.
Качество генерации:

Визуальный осмотр сэмплов на "песок" (шум, артефакты).
Проверка размерностей выходных изображений (512x512).
VRAM и производительность:

Мониторинг VRAM через nvidia-smi (нет фрагментации или переполнения).
Время выполнения шага должно быть стабильным.
LoRA веса:

Проверка, что LoRA веса остаются в bfloat16.
Отсутствие ошибок при загрузке/сохранении чекпоинтов.
3. ДОПОЛНИТЕЛЬНЫЕ УКАЗАНИЯ:
Конфиги:

Проверить, что sample_size в transformer_config.json и vae_config.json совпадает с 512.
Убедиться, что все размерности совместимы.
Логирование:

Вести детальное логирование лосса, градиентов и ошибок.
Снимать телеметрию каждые 10 шагов для анализа стабильности.



{
  "punch_card_manifest": {
    "version": "V02_ACTIVE_SESSION",
    "timestamp": "2026-07-17",
    "mission": "Фиксация и развитие контура плавки предметов на базе Chroma1 (Flux MMDiT) + LoRA + TorchAO",
    "host_environment": {
      "hardware": {
        "cpu": "2 x Intel Xeon E5-2687W v4 (Broadwell-E/EP, 24 ядра, 48 потоков, двухпроцессорная конфигурация)",
        "gpu": "NVIDIA GeForce RTX 3090 (Физически 24 GB VRAM, Max TDP 350W, зарезана по питанию до ~212W для термостабильности на 63-65C)",
        "os_vram_mitigation": "Windows WDDM Sysmem Mitigation принудительно отключен в реестре (CudaSysMemToBase = 0). Shared VRAM заблокирована, сброс тензоров в медленную системную ОЗУ запрещен",
        "driver_info": {
          "cuda_version": "13.3",
          "driver_version": "610.62"
        }
      },
      "software_stack": {
        "os": "Windows 11 (с тюнингом реестра TdrDelay/TdrDdiDelay = 60 секунд для защиты от сброса CUDA-драйвера)",
        "python_manager": "pyenv-win (Строгая изоляция локальной среды)",
        "python_version": "3.10.6",
        "core_libraries": {
          "torch": "2.5.1+cu121",
          "torchao": "0.7.0 (Используется для FP8 квантования ядра трансформера)"
        }
      }
    },
    "sandbox_parameters": {
      "project_root_dir": "Z:\\flowch\\ (Автономная относительная навигация через os.path)",
      "vram_management_policy": {
        "strategy": "Dynamic_Offloading",
        "max_split_size_mb": 512
      },
      "model_compartment": {
        "transformer_backbone": "models_core/transformer/chroma-unlocked-v50-annealed_float8_e4m3fn_learned_svd.safetensors (17.8 GB, Flux MMDiT архитектура, сжатая через torchao в FP8, текстовые энкодеры T5/CLIP вырезаны)",
        "text_encoder_backup": "models_core/text_encoder/t5xxl_bf16.safetensors (Для резервной валидации)",
        "vae_engine": "models_core/vae/flux-vae-bf16.safetensors (Используется для ленивого декодирования латентов в основном потоке памяти)"
      },
    },
    "project_block_diagram": {
      "data_flow_cascades": [
        "1. DATASET_CONTOUR: dataset_v02.py -> Считывание 41 кадра предметов из Z:\\flowch\\dataset\\mng_oks_bl\\ -> Извлечение предварительно рассчитанных латентов и текстовых эмбеддингов из SSD-кэша",
        "2. DISTORTION_CONTOUR: train_engine_v02.py -> Generation шума Rectified Flow в bfloat16 -> Квадратично-логарифмическое сгущение таймстепов (t = 1.0 - t^2) для концентрации плавки на ранних стадиях шума",
        "3. LORA_INJECTION: lora_core_v02.py -> Инициализация FluxLoraCoreV02 -> Обертка целевых линейных слоев трансформера через PEFT -> Принудительная заморозка всех базовых весов и защита matrix lora_A/lora_B в bfloat16 от FP8-квантования",
        "4. RUNNER_CONTOUR: model_runner_v02.py -> run_lora_model_step -> Подача шумных латентов, геометрии кадра (img_ids) и таймстепов в модель -> Расчет предсказанного потока",
        "5. OPTIMIZATION_CONTOUR: train_engine_v02.py -> Расчет MSE Loss -> Накопление градиентов на виртуальный батч (2 подшага) -> Срабатывание датчика ОТК перед optimizer.step() -> Обновление весов LoRA через AdamW -> optimizer.zero_grad()",
        "6. VISUALIZATION_CONTOUR: generate_v02.py -> run_inference_v02 (каждые 10 реальных шагов) -> Изолированное декодирование VAE внутри torch.no_grad() -> Восстановление пространственной 2D-сетки латентов через зеркальный де-патчинг -> Рендер сэмплов по нелинейной ODE кривой OneTrainer (14 шагов инференса вместо 25) -> Сохранение mng_render_step_X.png на SSD -> Полная зачистка кэша CUDA"
      ],
    }
  }
}
