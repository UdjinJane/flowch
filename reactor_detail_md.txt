# 🌟 РЕАКТОР ДЛЯ ЗАПЕКАНИЯ LoRa: ДЕТАЛЬНОЕ ОПИСАНИЕ

## 1. Архитектура системы и потоки данных

```mermaid
graph TD
    %% Основные компоненты системы
    A["train_engine_v02.py"] --> B["lora_core_v02.py"]
    A --> C["model_runner_v02.py"]
    A --> D["dataset_v02.py"]
    A --> E["generate_v02.py"]

    %% Структура и потоки данных
    subgraph SRC_DIR ["/src/"]
        A -->|Шаг обучения| B
        A -->|Пайплайн| G1["chroma_pipeline.py"]
        A -->|Рендеринг| E
    end

    subgraph CACHE ["Хранилище (Z:\\flowch\\)"]
        P["\\cache\\latent_embeds"] -->|Загрузка| A
        P2["\\text_cache (Monolithic)"] -->|T5+CLIP| E
        OUT["\\output\\images\"] <--|PNG| E
    end

    %% Ограничения и режимы
    VRAM["VRAM: <21.0 GB"]
    B --> VRAM
    P2 -->|Текстовые монолиты| A

    %% Стили для визуализации
    style A fill:#ff9,stroke:#333,color:black
    style B fill:#9f6,stroke:#333,color:black
    style C fill:#6f9,stroke:#333,color:black
    style P2 fill:#f96,stroke:#333,color:black
    style VRAM fill:#ff0,stroke:#333,color:black
```

## 2. Детальное описание модулей

### 2.1 train_engine_v02.py
**Функциональность:**
- **Загрузка данных:**
  - Из датасета `Z:\\flowch\\dataset\\mng_oks_bl\\metadata.jsonl`.
  - Кэширование latent-эмбеддингов (`cache\\latent_embeds`).
- **Цикл обучения:**
  - Контроль VRAM (<21.0 GB).
  - Логирование и отладка тахометра.

**Ключевые кодовые фрагменты:**
```python
# Пример контроля VRAM и лосса
current_loss = loss.item() * TrainConfig.GRADIENT_ACCUMULATION_STEPS
allocated_vram = torch.cuda.memory_allocated(device) / (1024 ** 3)
reserved_vram = torch.cuda.memory_reserved(device) / (1024 ** 3)
elapsed_time = time.time() - last_log_time
speed = 1.0 / elapsed_time if elapsed_time > 0 else 0.0
last_log_time = time.time()

console_msg = (
    f"[ОТК] Шаг: {global_step} | Эпоха: {epoch} | "
    f"MSE Лосс: {current_loss:.4f} | Скорость: {speed:.2f} it/s | "
    f"VRAM Active: {allocated_vram:.2f} GB | Reserved: {reserved_vram:.2f} GB"
)
print(console_msg)
```

### 2.2 lora_core_v02.py
**Функциональность:**
- **8-битная оптимизация AdamW:**
  - Модуль `ao_optim_monolith_v02.py` перехватывает 38 обучаемых LoRa-тензоров.
  - Сжимает моменты градиентов из `float32` в `int8`.
  - Динамический кастинг:
    ```python
    # Метод step() для расчета шага
    def step(self):
        self._cast_to_float32()  # Разжимаем на 1 мс
        # Расчет шага
        self._cast_to_int8()     # Герметизация обратно
    ```
- **Контроль фрагментации VRAM:**
  - Экономия до 2-3 ГБ VRAM.

### 2.3 model_runner_v02.py
**Функциональность:**
- **Запуск и управление моделью Chroma Unlocked v50.**
  - Геометрия RoPE (1280 токенов сетки без батч-размерности).
  - Взаимодействие с текстовым энкодером (`t5xxl_bf16.safetensors`) и VAE-модулем (`flux-vae-bf16.safetensors`).

### 2.4 generate_v02.py
**Функциональность:**
- **Decode latents (VAE).**
- **Postprocessing PNG.**
- **Статистика скорости (it/s).**
- **Фиксация изображений на SSD:**
  ```python
  output_path = os.path.join(TrainConfig.OUTPUT_DIR, "images", f"mng_render_step_{current_step}.png")
  os.makedirs(os.path.dirname(output_path), exist_ok=True)
  final_image.save(output_path)
  print(f"[ТРИУМФ V07] Чистокровное изображение запечено на SSD: {output_path}")
  ```

## 3. Ключевые ограничения и режимы работы
- **VRAM:** Жесткий лимит — 21.0 ГБ (OOM при превышении).
- **Режим работы:** Боевой (`USE_EMULATORS = False` по умолчанию).

## 4. Ключевые ограничения и правила кодирования
1. **Самоимпорт:** Категорически запрещен в шапке.
2. **Хардкодные пути:** Запрещены `sys.path`.
3. **Правки математики:** Вносятся поблочно без заглушек (`TODO`, `pass`, троеточий).
4. **Вангование:** Запрещено, верить только приборам Кэпа.

## 5. Примеры кода и фрагменты

### 5.1 Контроль лосса и VRAM (train_engine_v02.py)
```python
# Расчет среднеквадратичной ошибки мантиссы
loss_active = (F.mse_loss(pred_tensor_64, target_flow, reduction="none") * weight_mask).mean()

# Обратное распространение во float32-контуре для защиты от Underflow
(loss_active / TrainConfig.GRADIENT_ACCUMULATION_STEPS).backward()
```

### 5.2 Динамический кастинг градиентов (lora_core_v02.py)
```python
# Метод step() для расчета шага
def step(self):
    self._cast_to_float32()  # Разжимаем на 1 мс
    # Расчет шага
    self._cast_to_int8()     # Герметизация обратно
```

## 6. Инструкции по работе и настройке
- **Боевой режим:** Активируется по умолчанию (`USE_EMULATORS = False`).
  - Реальная загрузка весов VAE и трансформера.

## 7. Чекпоинтинг и логгирование
```python
# Чекпоинтинг каждые N шагов
if global_step % TrainConfig.SAVE_STEPS == 0:
    print(f"[Т] Чекпоинт на шаге {global_step}...")

# Телеметрия (средний MSE)
if global_step % 10 == 0 and len(telemetry.loss_buffer) > 0:
    avg_loss = sum(telemetry.loss_buffer) / len(telemetry.loss_buffer)
    print(f"\n 📡 [ТЕЛЕМЕТРИЯ] Шаг: {global_step} | MSE: {avg_loss:.6f}")
    telemetry.flush_aggregated_log(global_step, epoch)
```